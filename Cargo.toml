[workspace]
resolver = "2"
members = [
    "bin",
    "core",
    "modules/*",
    "types/*",
]
exclude = ["doctest", "e2e"]

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
