[package]
name = "ferron"
version = "3.0.0-beta.1"
edition = "2021"

[dependencies]
ferron-entrypoint = { path = "../entrypoint", features = ["profile-default"] }

[build-dependencies]
winresource = "0.1.19"
