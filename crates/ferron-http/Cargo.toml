[package]
name = "ferron-http"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
tokio = { version = "1", features = ["full"] }
http = "1"
parking_lot = "0.12"

ferron-common = { path = "../ferron-common" }
ferron-registry = { path = "../ferron-registry" }
