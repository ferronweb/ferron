[package]
name = "ferron-admin-api"
version = "3.0.0-alpha.1"
edition = "2021"

[dependencies]
axum = "0.8"
serde_json = "1.0"
tokio = { version = "1", features = ["net", "rt", "sync", "macros"] }
tokio-util = "0.7"
arc-swap = "1.7"

ferron-core = { path = "../../core" }
