[package]
name = "ferron-dns-stalwart"
version = "3.0.0-beta.8"
edition = "2021"

[dependencies]
ferron-core = { path = "../../core" }
ferron-dns = { path = "../../types/dns" }
dns-update = { git = "https://github.com/stalwartlabs/dns-update", rev = "bae0b909c9419f2b6d155cfd750adcc0d1d17188" } # Use upstream with updated `quick-xml` instead of crates.io version (that doesn't have that fix yet)
async-trait = "0.1.89"
base64 = "0.23.0"
hex = "0.4.3"
anyhow = "1.0.102"
hyper = "1.9.0"
