[package]
name = "ferron-observability-statsd"
version = "3.0.0-beta.10"
edition = "2021"

[dependencies]
rustc-hash = "2.1"
ferron-observability = { path = "../../types/observability", features = ["module"] }
ferron-core = { path = "../../core" }

async-channel = "2.5.0"
tokio = { version = "1", features = ["net", "rt", "sync", "macros", "time"] }
tokio-util = "0.7"
